# 咪菲認字小花園 Design File

## 產品定位

「咪菲認字小花園」是一個給 6 歲小朋友使用的繁體中文認字練習 app。第一階段重點是普通話聽音選字，讓孩子用手機或平板每天完成短時間、可累積的認字練習。

這不是補習班式的測驗工具，而是一個溫柔、穩定、低壓力的家庭練習空間。

## 主要使用者

- 小孩：在手機或平板上練習認字，不需要使用電腦。
- 家長：建立課程、管理不同小孩、查看每週星星進度。

## 目前已完成

- App 名稱：咪菲認字小花園
- 音訊練習：聽普通話朗讀，選出正確繁體字詞。
- 系統朗讀：目前繼續使用瀏覽器 / 系統語音，不使用預錄音檔。
- 可調整練習長度：預設每次練習 15 題，家長可在設定頁調整。
- 星星獎勵：完成一輪練習得到 1 顆星，每天最多 3 顆星。
- 今日任務：顯示今日星星進度，滿 3 顆星後顯示今日任務已完成。
- 額外練習：今天拿滿 3 顆星後，小孩仍可繼續練習，但不再增加星星。
- 多小孩 Profile：家長可以建立不同小孩，各自記錄每週學習。
- 多課程：每個小孩各自可以建立最多 10 組字詞課程。
- 每個小孩有自己的練習題數設定。
- 課程選擇：練習前可以選小孩和課程，不再把所有課程混在一起。
- 設定頁：把原本「家長」改為「設定」，用來管理小孩與課程。
- Google 登入（2026-07）：拿掉家庭密碼，改用 Google 帳號登入 + email 白名單（目前只有 yopllit@gmail.com）。白名單同時寫在 `app.js`（前端擋畫面）和 `firestore.rules`（真正的資料保護）。
- Firebase 同步：課程、小孩資料、題數和星星會同步到 Firestore，並保留 localStorage 作為本機備份。
- 部署：正式版已部署在 Vercel。

## 目前頁面結構

### 練習

練習頁是孩子主要使用的畫面。

資訊順序：

1. 選擇小孩
2. 選擇課程
3. 查看今日任務與星星
4. 按「開始練習」
5. 聽音選字
6. 完成設定題數後，答錯次數在標準內就得到 1 顆星

UX 調整方向：

- 把「今天誰要練習」放在最前面，先選小孩與課程。
- 今日任務獨立成一張任務卡，讓星星目標清楚但不壓迫。
- 練習卡只放當下要做的事：開始、再聽一次、選答案。
- 課程與小孩選擇不藏在題目中間，避免孩子開始後被設定干擾。

答題規則（2026-07 調整）：

- 每題最多 2 次機會：答錯一次可以再選一次，第二次還是錯就直接公布正確答案，進入下一題（避免小孩用「刪去法猜」也能過關）。
- 一輪（依設定題數）結束後，答錯題數超過 2 題（即 3 題以上）就不給星星，但仍完整跑完這一輪、看得到「本輪答對」次數，鼓勵再試一次。
- 進度條和題號以「已作答題數」計算，不是「答對題數」，確保答錯題也會正常往下一題走，輪次一定會結束。

重要狀態：

- 尚未開始：顯示開始練習按鈕。
- 練習中：顯示題目進度、再聽一次、答案選項、下一題。
- 完成一輪且答錯 ≤2 題：顯示撒花與「恭喜完成第 X 個練習」。
- 完成一輪但答錯 ≥3 題：不顯示撒花，顯示「這次先不算星星，再試一次吧！」，不給星星。
- 今日完成：滿 3 顆星後顯示完成狀態，但仍可按「繼續練習」。

### 本週集點

本週集點頁讓家長和小孩看到一週七天的星星進度。

規則：

- 每天最多 3 顆星。
- 每完成一輪練習得到 1 顆星。
- 每個小孩的星星分開記錄。

UX 調整方向：

- 小孩選擇放在最上面。
- 標題使用目前小孩名稱，例如「咪菲的中文集點卡」。
- 一週七天要能在手機上一次看完，不需要往下滑才知道整週狀況。
- 額外顯示本週總星星，例如「本週 8 / 21 ⭐」。

### 設定

設定頁是家長使用的管理頁。

目前功能：

- 新增 / 刪除小孩
- 修改小孩名稱
- 新增 / 刪除課程
- 修改課程名稱
- 編輯課程字詞
- 設定每次練習題數，預設 15 題
- Firebase 自動同步課程與星星。

設計方向（2026-07 重新設計）：

- 三層串接下拉選單：小孩 → 課程 → 字詞內容。選小孩會即時換課程選單的選項，選課程會即時換下方字詞編輯區的內容，不用 1-2-3-4 步驟編號（家長平常是重複使用，不是每次照順序走一遍）。
- 小孩、課程放在同一張「小孩與課程」卡片，字詞編輯放另一張「課程內容」卡片。
- 切換小孩時，課程列表、每次題數、字詞編輯都跟著切換。
- 頁面上方一行「設定情境列」（例如「菲菲・入門詞」）取代原本散落在各卡片裡的說明文字，一眼看到目前編輯對象。
- 小孩改名／刪除收進「重新命名或刪除小孩」的收合區，預設收起，不干擾常用操作。
- 課程字詞直接在編輯區管理，不另外做預覽區塊。
- 不使用太多後台感的表格；手機上每個操作區塊要清楚分開。

## 視覺方向

- 溫柔、乾淨、兒童友善。
- 以綠色作為主要行動色，黃色星星作為獎勵色。
- 不使用太多裝飾，避免分散孩子注意力。
- 圓角保持小而清楚，像工具型 app，不像行銷 landing page。
- 文字要大、按鈕要好按，適合手機和平板。

## 互動原則

- 進入 App 先用 Google 帳號登入（白名單以外的帳號會被拒絕）；登入後 Firebase 在背景同步。
- 孩子畫面只保留必要操作。
- 家長管理放到設定頁，不混在練習流程中。
- 答錯時可以改選，選對後錯誤狀態不應一直干擾。
- 完成狀態要清楚，不要在已完成時還提示開始。
- 聲音要穩定：下一題、再聽一次都應該能朗讀。

## 目前不做

- 暫時不做多家庭帳號系統或動態白名單管理介面；白名單目前是寫死在程式碼裡的固定清單。
- 暫時不做看圖認字。
- 暫時不做找一樣。
- 暫時不使用 AI TTS 預錄每個字。

## Backlog

具體待辦與優先順序請看 [BACKLOG.md](./BACKLOG.md)。

## 正式網址

https://miffy-chinese-garden.vercel.app
